info:
  name: createWorkout
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/workouts"
  body:
    type: json
    data: |-
      {
        "name": "Upper Body",
        "duration": 1000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
