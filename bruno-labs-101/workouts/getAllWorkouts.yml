info:
  name: getAllWorkouts
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/workouts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
