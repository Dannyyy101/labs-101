info:
  name: getAllExercises
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/workouts/exercises"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
