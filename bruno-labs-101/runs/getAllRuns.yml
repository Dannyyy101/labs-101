info:
  name: getAllRuns
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/workouts/sync"
  body:
    type: json
    data: |-
      {
        "uuid" : "1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
