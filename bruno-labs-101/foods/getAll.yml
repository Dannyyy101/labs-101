info:
  name: getAll
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/foods?page=0"
  params:
    - name: page
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
