info:
  name: findByBarcode
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/foods/bar-code/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
